package io.alexheld.mockserver.logging


/*


open class LogDTO(override var content: MutableMap<String, Any?> = mutableMapOf()) : io.alexheld.mockserver.logging.WithLogMetadata {

    constructor(id: String, timestamp: Instant, type: LogMessageType, properties: Map<String, Any?> = mutableMapOf()) : this(
        mutableMapOf<String, Any?>(
            "id" to id,
            "timestamp" to timestamp,
            "type" to type.name
        ).appendWith(properties)
    )
}
*/
