package io.alexheld.mockserver.logging

/*

open class ContentMapBase  : Map<String, Any?>, WithContentMap {

    override val content: MutableMap<String, Any?>

    constructor(content: MutableMap<String, Any?>)  {
        this.content = content
    }


    override val entries: MutableSet<MutableMap.MutableEntry<String, Any?>>
        get() = content.entries

    override val keys: MutableSet<String>
        get() = content.keys

    override val size: Int
        get() = content.size

    override val values: MutableCollection<Any?>
        get() = content.values

    override fun containsKey(key: String): Boolean = content.containsKey(key)

    override fun containsValue(value: Any?): Boolean = content.containsValue(value)

    override fun get(key: String): Any? = content[key]

    override fun isEmpty(): Boolean = content.isEmpty()
}
*/


